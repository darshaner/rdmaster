all:
  children:
    other_hosts:
      hosts:
        rf.local:
        debian.local:
    docker_builder:
      hosts:
        rdmaster.local:
    docker_host_runner:
      hosts:
        rdprod01.local:
    docker_registry_host:
      hosts:
        rdprod01.local:
    haproxy_host:
      hosts:
        haproxy.local:
    docker_host_runner_lab:
      hosts:
        rdlab01.local: